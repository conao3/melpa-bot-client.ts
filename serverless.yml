service:
  name: melpa-bot-client

# Add the serverless-webpack plugin
plugins:
  - serverless-webpack

provider:
  name: aws
  runtime: nodejs8.10

functions:
  entry:
    handler: handler.entry
    events:
      - http:
          method: any
          path: entry
